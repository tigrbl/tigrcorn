# Certification tools

This directory holds mutable helper tooling for certification and control-plane checks that are not frozen release artifacts.

Current tools:

- `status.py` - summarize the current release-gate and promotion-target state for CI and maintainer checkpoints
