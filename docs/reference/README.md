# Reference Schemas

This directory holds short-path mutable schemas and reference-format contracts.

Current primary file:

- `risk_register.schema.json` - schema for the Phase 8 machine-readable risk register
