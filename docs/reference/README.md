# Reference Schemas

This directory holds short-path mutable schemas and reference-format contracts.

There is no active reference schema in this directory now. Legacy standalone risk-register schema material was removed when risk authority moved fully into `.ssot/registry.json`.
