# Strict validation bundle

This bundle preserves the exact strict-validation commands and outputs for the canonical 0.3.9 release root.

- targeted strict-validation pytest suite: `27 passed`
- broader certification-refresh pytest matrix: `57 passed`
