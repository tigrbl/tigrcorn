# Phase 4 advanced delivery artifacts

This directory captures the public support statement for the Phase 4 delivery surface and the later boundary decision that formalized the certifiable RFC envelope.

Artifacts included here:

- `early_hints_support_statement.json`
- `alt_svc_support_statement.json`
- `runtime_compatibility_matrix.json`
- `examples_matrix.json`

Current boundary decision:

- `RFC 8297` is a current local-conformance direct-delivery target.
- `RFC 7838 §3` is a current local-conformance bounded Alt-Svc header-field target.
- `RFC 9218` remains outside the current package boundary.
