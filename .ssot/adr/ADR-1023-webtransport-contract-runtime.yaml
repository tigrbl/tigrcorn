schema_version: "0.1.0"
kind: "adr"
id: "adr:1023"
number: 1023
slug: "webtransport-contract-runtime"
title: "implement WebTransport as a contract-native runtime surface"
status: "accepted"
origin: "repo-local"
decision_date: null
tags: []
summary: "Decision: WebTransport shall be implemented as a contract-native Tigrcorn runtime surface."
supersedes: []
superseded_by: []
status_notes: []
references:
  - "W3C WebTransport Working Draft 25 March 2026: https://www.w3.org/TR/2026/WD-webtransport-20260325/"
  - "W3C WebTransport latest published version: https://www.w3.org/TR/webtransport/"
  - "IETF draft-ietf-webtrans-http3 WebTransport over HTTP/3: https://datatracker.ietf.org/doc/draft-ietf-webtrans-http3/"
  - "MDN WebTransport close method: https://developer.mozilla.org/en-US/docs/Web/API/WebTransport/close"
  - "MDN WebTransport closed property: https://developer.mozilla.org/en-US/docs/Web/API/WebTransport/closed"
  - "MDN WebTransport stream references: https://developer.mozilla.org/en-US/docs/Web/API/WebTransport/createBidirectionalStream"
  - "MDN WebTransport datagrams reference: https://developer.mozilla.org/en-US/docs/Web/API/WebTransport/datagrams"
body: |-
  # ADR 1023 - WebTransport contract runtime

  Decision: WebTransport shall be implemented as a contract-native Tigrcorn runtime surface.

  WebTransport belongs in the native contract path because it depends on HTTP/3, QUIC streams, datagrams, session semantics, and transport metadata that ASGI3 does not standardize.
  
  Runtime behavior shall align with W3C WebTransport session, stream, datagram, close, abort, and cancel semantics while exposing those semantics through Tigrcorn's contract-native event surface rather than by pretending WebTransport is ordinary ASGI HTTP. MDN WebTransport documentation is informative input for examples and operator-facing descriptions of close, closed, stream, and datagram behavior; it is not the source of normative conformance.

  Consequences:

  - WebTransport is not modeled as ordinary ASGI HTTP.
  - ASGI3 exposure, if present, must be extension-based and capability-gated.
  - WebTransport tests must cover streams, datagrams, session lifecycle, and metadata.
