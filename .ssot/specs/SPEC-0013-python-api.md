# Python API

Stable imports:

```python
from ssot_registry.api import (
    load_registry,
    save_registry,
    validate_registry,
    plan_features,
    set_feature_lifecycle,
    plan_issues,
    evaluate_claims,
    verify_evidence_rows,
    freeze_boundary,
    certify_release,
    promote_release,
    publish_release,
    revoke_release,
    export_graph,
)
```
