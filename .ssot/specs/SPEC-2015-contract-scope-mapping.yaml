schema_version: "0.6.0"
kind: "spec"
id: "spc:2015"
number: 2015
slug: "contract-scope-mapping"
title: "Contract scope mapping"
status: "draft"
origin: "repo-local"
decision_date: null
tags: []
summary: "Tigrcorn shall define scope mapping between `tigr-asgi-contract` and ASGI/3 compatibility scopes."
supersedes:
  - "spc:2006"
superseded_by: []
status_notes:
  -
    status: "draft"
    note: "Updated scope mapping to broad contract scope types plus metadata extensions."
    at: "2026-05-27T17:40:36Z"
references: []
body: |-
  # Contract scope mapping
  
  Tigrcorn shall define scope mapping between `tigr-asgi-contract` and ASGI/3 compatibility scopes.
  
  Requirements:
  
  - Contract `http`, `websocket`, `webtransport`, and `lifespan` scopes map to the broad governed contract scope types.
  - HTTP/1.1, HTTP/2, HTTP/3, TLS, mTLS, ALPN, QUIC, and WebTransport capability facts are exposed as scope metadata or extension metadata, not as additional ASGI scope types.
  - Contract `webtransport` maps to the native contract path and only to ASGI/3 through documented extensions.
  - Contract metadata that has no ASGI/3 field maps through the extension bridge or fails closed.
  - Custom ad hoc scope types are not the compatibility mechanism.
  - Missing metadata must remain distinguishable from unsupported metadata.
spec_kind: "local-policy"
adr_ids:
  - "adr:1016"
